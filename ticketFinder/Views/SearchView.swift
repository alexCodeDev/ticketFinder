import SwiftUI

struct SearchView: View {
    var body: some View {
        Text("Поиск")
            .font(.largeTitle)
            .padding()
    }
}

struct SearchView_Previews: PreviewProvider {
    static var previews: some View {
        SearchView()
    }
}
